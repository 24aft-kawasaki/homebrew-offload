[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]

[dev-packages]
python-on-whales = "~=0.78.0"
python-dotenv = "*"

[requires]
# Support Python 3.14 and later
# Pipenv will match the current installed Python version
python_version = "3"
# Minimum required version: 3.14
# This is enforced at runtime in bin/brew-offload

[scripts]
# execute before test `pipenv sync --dev`
test = "python -m unittest tests.test_brew_offload"
